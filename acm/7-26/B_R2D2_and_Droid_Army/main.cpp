#include <iostream>
#include <vector>
using namespace std;
const int Max=1e+5+10;
int n,m,k;
int detail[5][Max];
bool caculate(int length,vector<int> &ans)
{
	for(int i=0;i<)
}
int main()
{
    while(cin>>n>>m>>k)
	{
		vector<int> ans[5];
		for(int i=0;i<n;++i)
			for(int j=0;j<m;++j)
				cin>>detail[j][i];
		int left=0,right=n;
		int mid=0,ans=0;
		while(right>=left)
		{
			mid=(left+right)/2;
			if()

		}
	}
    return 0;
}
