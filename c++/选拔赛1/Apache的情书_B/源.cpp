#include<iostream>
#include<string>
#include<regex>
using namespace std;
int main()
{
	string word;
	
	while (cin>>word)
	{
		int begin = 0;
		for (int i = 0; i < word.size; ++i)
		{
			if (isalpha(word[i]))
				continue;
			else
			{
				if(word.substr(0,))
			}
		}
	}
}